pkgname=tsuki-xfce4
pkgver=1.0
pkgrel=1
pkgdesc=""
arch=('x86_64')
license=('MIT')
depends=('xfconf' 'xfwm4')
source=()
md5sums=()

package() {
  install -dm755 "$pkgdir/etc"
  cp -r "$srcdir/etc" "$pkgdir/"
}
